@model HomeViewModel

<h1>Самые популярные туры</h1>
<div class="row mt-5 mb-2">
    @{ 
        foreach (Tour tour in Model.favTours)
        {
            @Html.Partial("_AllTour", tour)
        }
    }
</div>